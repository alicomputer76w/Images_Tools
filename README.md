Image Tools — privacy-friendly, fast image utilities. Client-side where possible; server-side worker for advanced tasks. Built with React + Node.

Run locally:
- `npm i`
- `npm run dev:web` in one terminal
- `npm run dev:server` in another terminal